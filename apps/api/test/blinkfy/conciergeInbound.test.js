const { validateInboundMessage, verifySecret } = require('../../src/services/blinkfy/conciergeInboundService');
describe('Concierge inbound messages', () => { it('validates safe inbound payloads', () => { expect(validateInboundMessage({ externalMessageId: 'm1', channel: 'linkedin', content: 'Hello' })).toEqual({ externalMessageId: 'm1', channel: 'linkedin', content: 'Hello' }); }); it('rejects missing content and invalid secret', () => { expect(() => validateInboundMessage({ externalMessageId: 'm1', channel: 'linkedin' })).toThrow('content'); expect(verifySecret('a', 'b')).toBe(false); }); });
