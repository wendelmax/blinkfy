# Blinkfy Concierge scheduling policy

Clients can persist a timezone and availability windows for assisted scheduling. Every policy response explicitly states that suggested slots require human approval and autonomous sending is disabled. Routes are scoped by workspace role and client access; this slice does not connect to a calendar provider or send invitations.
