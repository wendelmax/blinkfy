# Blinkfy Concierge — integration catalog

The client-scoped Concierge API exposes a read-only catalog of official ATS,
CRM, and n8n/MCP connectors. Every entry starts unconfigured, requires human
approval, and reports `transmitted: false`; no credentials or external calls
are accepted by this endpoint.
