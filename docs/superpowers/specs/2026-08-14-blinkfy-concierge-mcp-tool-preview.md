# Blinkfy Concierge — MCP tool preview

Authorized recruiters can validate a proposed MCP tool call and receive a
reviewable envelope. The preview includes the tool, scopes, client, and
arguments, but is always `approved: false`, `executed: false`, and
`transmitted: false`. No tool runner or external service is invoked.
