# Blinkfy Concierge follow-up kill-switch

Recruiters can configure an application-level follow-up cadence. The API persists the active sequence, returns a review-only plan, and automatically marks the sequence interrupted when any inbound message is received. The UI makes approval and no-autonomous-send guardrails explicit.
